[package]
name = "remoto"
version = "0.1.0"
description = "Remoto - control remoto de escritorio entre amigos"
authors = ["Kelvin Gomez"]
edition = "2021"

[lib]
name = "remoto_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
enigo = "0.6"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
strip = true
panic = "abort"
